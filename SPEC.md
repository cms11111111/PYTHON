# 食蛇遊戲 (Snake Game) Specification

## 概述
這是一個基於控制台 (Console) 的食蛇遊戲，移植自 Web 版本的邏輯。

## 功能需求 (Requirements)

### 1. 遊戲畫面
- **網格大小 (Grid):** 邏輯大小為 20x20 格。
- **顯示:** 使用 Console 字元繪製。
  - 牆壁: `+`, `-`, `|`
  - 蛇身: `[]` (綠色/深綠色)
  - 食物: `[]` (紅色)
  - 空白: (空格)

### 2. 遊戲控制
- **移動:** 方向鍵 (Up, Down, Left, Right)。
- **暫停:** 按 `P` 鍵切換暫停/繼續。
- **重新開始:** 遊戲結束後按 `Enter` 重新開始，按 `Esc` 退出。

### 3. 遊戲邏輯
- **速度選擇:** 遊戲開始前選擇速度。
  - 慢 (Slow): 150ms
  - 中 (Medium): 100ms
  - 快 (Fast): 50ms
- **分數:** 每吃一個食物得 10 分。
- **死亡條件:**
  - 撞牆 (Wall Collision)
  - 撞到自己 (Self Collision)
- **蛇的資料結構:**
  - 必須使用泛型串列 `List<Point>` 來管理蛇的身體節點。
  - `Point` 結構包含 X, Y 座標。

### 4. 實作細節
- **程式語言:** C# (.NET 10)
- **專案類型:** Console Application
- **關鍵技術:**
  - `List<T>` (Generics)
  - `System.Console` 繪圖 API
  - `Thread.Sleep` 控制遊戲迴圈速度

## 對應 Web 版功能
| 功能 | Web (js/game.js) | C# Console |
|------|------------------|------------|
| 畫布 | 400x400 (20px tiles) | 20x20 邏輯網格 |
| 速度 | 150/100/50 ms | 150/100/50 ms |
| 操作 | 按鈕 + 鍵盤 | 鍵盤選單 + 遊戲操作 |
| 暫停 | 按鈕 | P 鍵 |
